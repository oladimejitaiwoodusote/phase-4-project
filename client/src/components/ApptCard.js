import React from 'react'

function ApptCard({ id, type }) {
    return (
        <div>
            <span>{ id }</span>
            <span>{ type }</span>
        </div>
    )
    }

export default ApptCard